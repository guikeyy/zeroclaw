name: Scheduled Build ZeroClaw (ARMv7 Optimized)

on:
  schedule:
    - cron: '0 4 * * *' # 每天北京时间 12:00 运行
  workflow_dispatch:

jobs:
  build_official_latest:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须：用于创建 Release 和 Tag
    
    steps:
      - name: Checkout Upstream Master
        uses: actions/checkout@v4
        with:
          repository: zeroclaw-labs/zeroclaw
          ref: master
          fetch-depth: 1

      - name: Log Upstream Commit Info
        run: |
          echo "Building from upstream commit:"
          git log -1 --format="%H - %s (%cr)"

      - name: Determine Version and Beta Status
        id: get_version
        run: |
          # 1. 提取 Cargo.toml 中的原始版本
          RAW_VER=$(grep "^version =" Cargo.toml | head -1 | cut -d '"' -f 2)
          
          # 2. 获取上游官方最新的【正式稳定版】版本号
          UPSTREAM_STABLE=$(curl -s https://api.github.com/repos/zeroclaw-labs/zeroclaw/releases/latest | grep '"tag_name":' | sed -E 's/.*"v?([^"]+)".*/\1/')
          
          echo "Cargo.toml Version: $RAW_VER"
          echo "Upstream Stable Version: $UPSTREAM_STABLE"

          # 3. 判定逻辑
          IS_BETA="false"
          DISPLAY_VER="$RAW_VER"
          
          if [[ "$RAW_VER" == *"-beta"* ]] || [[ "$RAW_VER" != "$UPSTREAM_STABLE" ]]; then
            IS_BETA="true"
            if [[ "$RAW_VER" != *"-beta"* ]]; then
               DISPLAY_VER="${RAW_VER}-beta"
            fi
          fi

          echo "IS_BETA=$IS_BETA" >> $GITHUB_OUTPUT
          echo "VERSION=$DISPLAY_VER" >> $GITHUB_OUTPUT

      - name: Optimize Cargo.toml for Size
        run: |
          # 移除原有的 profile.release 及其之后的内容
          sed -i '/\[profile.release\]/,$d' Cargo.toml
          # 追加极致体积优化配置
          echo "" >> Cargo.toml
          echo "[profile.release]" >> Cargo.toml
          echo "opt-level = 'z'" >> Cargo.toml     # 极致体积优化
          echo "lto = true" >> Cargo.toml          # 全局链接优化
          echo "codegen-units = 1" >> Cargo.toml   # 牺牲编译速度换取最高优化
          echo "panic = 'abort'" >> Cargo.toml     # 移除异常回溯以减小体积
          echo "strip = true" >> Cargo.toml        # 剥离所有调试符号表
          echo "--- 修改后的 Cargo.toml 尾部 ---"
          tail -n 10 Cargo.toml

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: armv7-unknown-linux-gnueabihf

      - name: Install Cross
        run: cargo install cross --git https://github.com/cross-rs/cross.git

      - name: Build for ARMv7
        run: |
          cross build --target armv7-unknown-linux-gnueabihf --release

      - name: Package Binary
        run: |
          cd target/armv7-unknown-linux-gnueabihf/release
          VER="${{ steps.get_version.outputs.VERSION }}"
          FILENAME="zeroclaw-v${VER}-armv7-optimized.tar.gz"
          tar -czvf $FILENAME zeroclaw
          mv $FILENAME ../../../
          echo "FILENAME=$FILENAME" >> $GITHUB_ENV

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          name: Release v${{ steps.get_version.outputs.VERSION }} (Optimized)
          prerelease: ${{ steps.get_version.outputs.IS_BETA == 'true' }}
          files: ${{ env.FILENAME }}
          body: |
            ### 🚀 ZeroClaw ARMv7 自动构建 (体积优化版)
            本版本由 GitHub Actions 每日自动构建。

            **版本信息：**
            - **版本号**: `v${{ steps.get_version.outputs.VERSION }}`
            - **类型**: `${{ steps.get_version.outputs.IS_BETA == 'true' && '⚠️ 测试版 (Beta/Pre-release)' || '✅ 正式版 (Stable)' }}`
